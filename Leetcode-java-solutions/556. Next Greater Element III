class Solution{
    public int nextGreaterElement(int n){
        // take out all elements and form an array, if 0th element is greatest, then i will return -1 coz it is not at all possible to form greater than that, if it is not the greatest, sort the array in descending order and return ans...
        String a=""+n;
        char array[]=a.toCharArray();
        // {1,2};
        Arrays.sort(array);
        // asc...
        int ans=0;
        // desc orderr...
        for(int i=array.length-1;i>=0;i--){
            ans=ans*10+array[i]-'0';
        }
        if(ans<=n){
            return -1;
        }
        return ans;
    }
}